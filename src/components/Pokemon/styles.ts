import styled from "styled-components";

export const Container = styled.div`
    right: 0;
    top: 0;
    position: fixed;
    display: block;
    height: 100%;
    width: 40rem;

    img{
        display: block;
        position: relative;
        margin: auto;
        margin-top: 3rem;
        width: 12rem;
        height: 12rem;
        border: 1px solid black;
        background-image: url(../../grasstype.jpg);
    }
    border: 1px solid black;    
`